# 📈 Progress Tracker

> Har din ke baad yeh update hoga. Naya session shuru karte waqt **yeh sabse pehle padho**.

**Current status:** ✅ Day 22 done (Phase 5 🟠 Agent layer LIVE — /api/chat ab agentic, tool_used asli) → Next: Day 23 (React frontend — chat UI + upload flow, learner ka ghar 🏠)
**Last updated:** 2026-07-28
**⚠️ Handoff note:** Teaching style = concept-FIRST, then code; frontend (React/Angular/TS) analogies; Hinglish; never dump code without explaining "why". Go SLOW, let learner predict before revealing.
**📋 CAPSTONE PLAN:** Phase 5 (Day 19-25) ka full day-by-day plan = `docs/capstone-roadmap.md` (fresh session yahi padhe). Project = **SmartSupport SaaS** (RAG + agent + React). Vision: `docs/capstone-vision.md`. Diagram: `docs/capstone-arch.svg`.

**⚠️ NEXT SESSION first task:** Day 22 DONE (build-day, LIVE) — capstone DIFFERENTIATOR zinda. `/api/chat` ab AGENTIC (Day17-18 API me): flow seedha→branch, `tool_used` ab ASLI decision (Day21 me hardcode tha). Banaya: `agent/tools.py` — FACTORY `build_tools(company_id, state)` (agent tool ko sirf STRING deta par company_id+citations chahiye → closure me company_id bind + state dict me sources capture, React useCallback jaisa): `policy_search`(Day21 rag_answer wrap) + `account_api`(mock `_FAKE_ACCOUNTS` VJ-100/VJ-200); descriptions=LAKSHMAN REKHA (policy vs account cross-scope). `agent/agent.py` — `run_agent(q,company_id)->(answer,sources,tool_used)`: create_tool_calling_agent+AgentExecutor(return_intermediate_steps=True), tool_used=intermediate_steps ke action.tool se. 🐛 BUG+FIX: Claude output kabhi list-of-content-blocks (str nahi)→`.replace`/ChatResponse.answer fail → `_as_text()` join. `routes.py` /chat ab run_agent (plain answer() hata). TEST LIVE /api/chat: "EMI bounce"→policy_search+cite p2,p7; "VJ-100 balance"→account_api (₹12,500); "Mumbai mausam"→none honest. Koi nayi pip lib (langchain agent Day9/17-18). Ab **Day 23 REACT FRONTEND** (learner ka ghar): Vite+React+TS, `api/client.ts` (ApiClient+types.ts), Chat UI (msg list+input+citations+tool_used badge+loading/error/empty), Upload flow, CORS connect, browser e2e. Backend READY. ⚠️ Build-workflow: plan→code+test→note→push (4 files: day/, PROGRESS, README[badge+bullet], requirements — user verify karta hai!). Checklist: `docs/capstone-roadmap.md`. FUTURE: dedup (92 chunks). PENDING (Phase4): Day17-18 mentor comparison; course-sync pull. "Answer side" bana — koi naya RAG concept nahi, purane skills ka WIRING (Day7+8+9) API modules me. Threshold PROBE-then-tune (Day7): in-corpus ~0.42-0.56, out ~0.81+ → `DISTANCE_THRESHOLD=0.7`. Banaya: `retrieval/retriever.py` `retrieve(q, company_id, k=4)`=Chroma query + where company_id filter + threshold → [{text,page,doc_id,distance}]; `generation/rag_chain.py` `answer(q, company_id)->(text, sources)` DOUBLE grounding guard (retriever khaali→NOT_FOUND + prompt Claude context-bound), LCEL `prompt|ChatAnthropic(temp0)|StrOutputParser` (Day9), sources=text[:200]+page+doc_id; `api/routes.py` POST /chat→ChatResponse{answer,sources,tool_used}, tool_used MANUAL abhi (sources→"policy_search" warna "none"). TEST curl /api/chat: "EMI bounce"→₹1000+GST + cite p7,p2 + policy_search ✅; "pizza"→"jaankari nahi mili" + []+ none ✅. Gotcha: py3.9 f-string me `\` nahi. Ab **Day 22 AGENT** (differentiator, Day17-18): `agent/tools.py` policy_search(aaj ka RAG wrap)+account_api(mock live balance/EMI/order)+descriptions(LAKSHMAN REKHA); `agent/agent.py` create_tool_calling_agent+AgentExecutor; /chat ko agent se route → tool_used ASLI (agent chune). Test: policy Q→policy_search, "mera balance?"→account_api, "mausam?"→none. ⚠️ Build-workflow: plan→code+test→note→push. Checklist: `docs/capstone-roadmap.md`. FUTURE TODO: dedup (92 chunks=bajaj 2x). PENDING (Phase4): Day17-18 mentor comparison; course-sync pull. Banaya + LIVE tested: FastAPI server (`app/main.py` + CORS React-5173 + include_router prefix /api), `app/config.py` (paths, COLLECTION="smartsupport", .env copied from rag-mastery), `app/api/routes.py` (`GET /health` + `POST /upload`), `app/ingestion/pipeline.py` = `ingest_pdf(file_path, company_id)->(doc_id,n_chunks)`: PyPDFLoader(Day6)→clean→RecursiveCharacterTextSplitter 500/80(Day3)→Chroma(Day5) with metadata `{company_id,doc_id,source,page}`; **1 collection + company_id filter** (multi-tenant); embed=SentenceTransformerEmbeddingFunction(all-MiniLM-L6-v2). TESTED: bajaj PDF→46 chunks, "EMI bounce"→p2 ₹1000+GST; **isolation proved** (where company_id=bajaj→2, acme→0); `curl POST /api/upload -F file=@.. -F company_id=bajaj`→{doc_id,chunks:46,status:ok}. FastAPI free `/docs` Swagger. Installed: fastapi uvicorn python-multipart (capstone/backend/requirements.txt me). Ab **Day 21**: `retrieval/retriever.py` (Chroma search + company_id filter + threshold Day7 + opt rerank Day15) → `generation/rag_chain.py` (chunks+Claude→answer+citations Day8/9) → `POST /api/chat` PLAIN RAG first (agent Day22) → {answer, sources}. Test: "EMI bounce"→answer+page cite; "pizza"→not found. ⚠️ Build-workflow: plan→code+test→note→push. Full checklist: `docs/capstone-roadmap.md`. FUTURE TODO: dedup (same PDF re-upload=duplicate chunks; hireflow dedup.py, Day24 polish). PENDING (Phase 4): Day 17-18 mentor comparison (`05_Agentic_AI/`+`BFL_chatbot/app/bajaj_tools.py`); course-sync.md pull. Design + decisions LOCKED (learner ne roadmap me khud tick kiya): **repo = `rag-mastery/capstone/`** (alag repo nahi), **seed corpus = Bajaj policy** (Day 6 ke 40 chunks; SEED/dummy data hai, app ki limit nahi — product "koi bhi PDF upload"). Banaya: `capstone/` full skeleton (backend/app/{ingestion,retrieval,generation,agent,api,models} + frontend/src/{api,components,pages}); har backend module me docstring "kaunsa Day bharega + reuse". **API contract FINAL** = 2 mirror files: `backend/app/models/schemas.py` (Pydantic) ⇄ `frontend/src/api/types.ts` (TS) — 3 endpoints: POST /api/upload{file,company_id}→{doc_id,chunks,status}; POST /api/chat{company_id,question}→{answer,sources[],tool_used}; GET /api/health. `capstone/README.md` = architecture doc. Ab **Day 20**: FastAPI `main.py`+CORS+/api/health, phir `ingestion/pipeline.py` (upload→clean→chunk→embed→Chroma w/ company_id+page metadata) + POST /api/upload. Reuse Day 6+3+5. ⚠️ Build-phase workflow: concept-days ka "scratch+library 2 files" ab NAHI — har build-day: plan→code+test→note update→push. Full checklist: `docs/capstone-roadmap.md`. PENDING TODO (Phase 4 se, kabhi bhi): Day 17-18 mentor comparison (`05_Agentic_AI/` + `BFL_chatbot/app/bajaj_tools.py`); weekend course-sync.md pull.

---

## ✅ Completed Days

- **Day 22** — 🟠 Agent layer (differentiator, Day 17-18) (build-day, LIVE). `/api/chat` ab AGENTIC — flow seedha→branch, `tool_used` ASLI decision (Day21 hardcode tha). Design challenge: agent tool ko sirf STRING deta par tool ko company_id+citations chahiye → **FACTORY+closure** (`build_tools(company_id, state)`; company_id closure me bind, sources state dict me capture — React useCallback jaisa). `agent/tools.py`: policy_search(Day21 RAG wrap)+account_api(mock VJ-100/VJ-200); desc=LAKSHMAN REKHA. `agent/agent.py`: create_tool_calling_agent+AgentExecutor(return_intermediate_steps), tool_used=action.tool. 🐛 BUG: Claude output kabhi list-of-blocks (str nahi)→crash→`_as_text()` fix. `routes.py`/chat→run_agent. TEST LIVE: EMI→policy_search+cite; VJ-100 balance→account_api; mausam→none honest. Koi nayi lib. Next Day23: React frontend.

- **Day 21** — 🟠 Retrieval + Generation → POST /api/chat (build-day, LIVE). "Answer side". Koi naya RAG concept nahi — WIRING (Day7 threshold + Day8 grounding/citations + Day9 LCEL) API me. Threshold PROBE-then-tune (Day7 sabak): in-corpus dist ~0.42-0.56, out-of-corpus ~0.81+ → `DISTANCE_THRESHOLD=0.7` (pizza/weather auto-reject). `retrieval/retriever.py`=Chroma query + `where company_id` (tenant isolation) + threshold → [{text,page,doc_id,distance}]. `generation/rag_chain.py`=`answer(q,company_id)->(text,sources)`, **DOUBLE grounding guard** (retriever khaali→NOT_FOUND + prompt Claude context-bound=chunk aaye par jawab na ho tab bhi honest), LCEL `prompt|ChatAnthropic(temp0)|StrOutputParser`. `routes.py` POST /chat→ChatResponse; tool_used MANUAL (sources→policy_search, warna none; Day22 agent asli karega). TEST LIVE curl: "EMI bounce"→₹1000+GST + cite p7,p2 + policy_search ✅; "pizza"→"jaankari nahi mili" + [] + none ✅ (no hallucination). Gotcha: py3.9 f-string `\` nahi. User ka --reload server hot-reload se serve kiya. Code `capstone/backend/`. Next Day22: agent + tools.

- **Day 20** — 🟠 Backend skeleton + Ingestion pipeline (build-day, LIVE). App ka "upload side" bana. NAYA concept: **FastAPI = Python ka Express/Nest** (`@app.get`, dict→JSON auto, `uvicorn --reload`≈nodemon, FREE `/docs` Swagger, Pydantic `response_model`=RUNTIME validation=TS types+enforce→galat shape 422). Banaya: `main.py` (app+CORS 5173+include_router /api), `config.py` (paths, COLLECTION="smartsupport", .env), `routes.py` (/health + POST /upload), `ingestion/pipeline.py` = `ingest_pdf(path, company_id)->(doc_id, n)`: PyPDFLoader(Day6)→clean(khali lines squeeze)→splitter 500/80(Day3)→Chroma(Day5) metadata `{company_id, doc_id, source, page}`; **1 collection + company_id filter** (multi-tenant, alag collection nahi); doc_id=uuid4[:8]; embed=SentenceTransformerEmbeddingFunction(all-MiniLM). TEST LIVE: bajaj→46 chunks, EMI bounce→p2 ₹1000+GST; **isolation proved** (company_id=bajaj→2 hits, acme→0 🔒); `curl POST /api/upload`→{doc_id, chunks:46, status:ok}, Chroma=46. Installed fastapi/uvicorn/python-multipart. Sab code `capstone/backend/`. Koi diagram/exercise (build-phase). FUTURE: dedup (re-upload=duplicate). Next Day 21: retrieval+generation→POST /api/chat.

- **Day 19** — 🟠 PHASE 5 shuru: Design & Architecture (NO heavy code). Aaj blueprint banaya (frontend dev comfort zone: "pehle TS interface + API contract, phir eent"). DECISIONS locked: repo=`rag-mastery/capstone/`, seed corpus=Bajaj policy (Day 6 ke 40 chunks — ye SEED/dummy data hai, app ki limit nahi; product "koi bhi PDF upload"). BIG CONCEPT: **API contract = single source of truth** — backend Pydantic (`models/schemas.py`) ⇄ frontend TS (`api/types.ts`) = ek shape ke 2 sheeshe, dono match. 3 endpoints: /api/upload, /api/chat (→ answer+sources+tool_used), /api/health. Banaya: `capstone/` full hireflow-style skeleton (backend/app/{ingestion,retrieval,generation,agent,api,models} + frontend/src/{api,components,pages}); har backend module me docstring "kaunsa Day bharega + kaunsa purana Day reuse" (ingestion=6/3/2/5, retrieval=7/15, gen=8/9, agent=17-18) → capstone = poore Day 1-18 ka sab ek app me; naya sirf FastAPI layer + React + jodna. Agent differentiator (3 raaste: policy_search/account_api/none) yaad. `capstone/README.md`=arch doc. Koi diagram/exercise nahi (build-phase). Next Day 20: FastAPI + ingestion pipeline.

- **Day 17-18** — 🟣 PHASE 4 FINALE: Agentic RAG (RAG + tool use), scratch + library DONE. Seedhi rassi (Day 8: query→retrieve→Claude, har baar same) → **agent loop**: LLM KHUD decide karta kaunsa tool + kya input, tool chalta, result wapas LLM ko, phir zaroorat ho to dobara tool warna final ANSWER. **RAG ab ek TOOL hai** (`policy_search`) doosre tools ke saath (`account_balance` = fake live API). Tool ki 3 cheez: name + **description (LLM ka manual: role+input+LIMIT)** + function. Asli "jaadu" (learner ne khud pakda): LLM sirf RAW STRING deta → hum PARSE karke sahi Python fn chalate (scratch: `TOOL: x | INPUT: y` regex; library: Claude native tool-calling). LIVE scratch: 3 query 3 raaste (EMI→policy_search 2-step, VJ-100 balance→account_balance 2-step + **input KHUD nikaala**, mausam→koi tool nahi 1-step); combined query (charge+balance)→3-step (tool order LLM decide karta, fixed nahi); VJ-999 (account nahi)→tool "nahi mila"→agent ne jhooth nahi bola=**self-correcting** (Day8 threshold+Day12 faithfulness ab agent ke andar). Library (File 2): 3 cheez di — `@tool` (docstring=description), `ChatAnthropic` (tool-calling capable), prompt + `{agent_scratchpad}` (=scratch messages[]); `create_tool_calling_agent`+`AgentExecutor(verbose=True)` → loop andar chhupa (Day15/16 jaisa), verbose me `Invoking: policy_search with {...}` = scratch ka TOOL:...INPUT:. 🐛 2 BUG-EXPERIMENT: (A) jhoothi/colliding desc (balance tool ne bola "main policy ka hoon") → Claude ne DONO baar SAHI chuna (naam+saare tools cross-check=robust; "kharab desc=pakka galat" GALAT dawa); (B) under-scoped desc (`policy_search`="sirf foreclosure, EMI bounce NAHI") → EMI query pe tool CHALAYA HI NAHI → apni yaaddasht se jawab=HALLUCINATION (grounding toota) = reliable failure. Seekh: description = LAKSHMAN REKHA (na tight na dheeli). Koi NAYI lib nahi (sab langchain Day 9). 1 diagram (agent loop vs seedhi rassi). Exercise (A/B/C/D+bonus) learner ne solve kiya. TODO: mentor comparison (05_Agentic_AI + BFL_chatbot/bajaj_tools.py).

- **Day 16** — 🟣 Multi-Query + Parent-Child chunking, scratch + library DONE. **Multi-Query:** 1 query → LLM se 3-4 ROOP → sab search → merge(unique). Recall↑ par cost↑ (extra LLM call). **Parent-Child:** trade-off jugaad — search CHILD (chhota=सटीक embedding) par LLM ko do PARENT (bada=pura context). "Dhoondha child se, diya parent" (Day14/15 pattern: search alag, dena alag). INGESTION do-level split: doc → split#1 bade PARENTS → docstore(`dict`/InMemoryStore, NO embedding) → split#2 chhote CHILDREN → Chroma me embed; har child me `parent_id` pointer (foreign key). Retrieve: child search → `parent_id` se docstore lookup (search NAHI, `dict[pid]`) → merge unique. LIVE bajaj corpus: query "EMI bounce charge?" → 4 children (P-0-c1,c3,P-1-c2,c3) → 2 unique parents (P-0,P-1); answer me "agli EMI ke saath"+"CIBIL girta" bhi aaya (=parent ka pura context, chhote child me nahi tha → PC ka point). Library (File 2): `ParentDocumentRetriever`(vectorstore=Chroma children, docstore=InMemoryStore parents, child_splitter, parent_splitter=None→har doc=1 parent) + `MultiQueryRetriever.from_llm(retriever=parent_retriever)` (nest = roop→child-search→parents→merge). 🐛 CATCH: MultiQueryRetriever default parser har newline=query → LLM ki intro line "Here are 3 versions:" bhi query bani → foreclosure (kachra) doc aaya (precision↓). Scratch me nahi aaya (humne strip kiya=scratch-first fayda). FIX (file+notes me): custom `CleanLineParser` (`:` end/`?` na wali line chhaan) + `prompt|llm|parser` LCEL → `MultiQueryRetriever(llm_chain=clean_chain)` → 3→2 parents, foreclosure gayab. 2 diagram (combined flow + ingestion). Exercise banaya. Simple chunking (Day1-15)=ek split, search chunk=LLM chunk; PC=alag. Optional tools (eval se prove). NOTE: koi nayi pip lib nahi.

- **Day 15** — 🟣 Re-ranking (2-stage retrieval), scratch + library DONE. Stage 1 bi-encoder cosine → shortlist (extra docs); Stage 2 cross-encoder (query+doc EK SAATH → score) → re-sort. Library (File 2): LangChain `ContextualCompressionRetriever`(base=`as_retriever(k=4)`) + `CrossEncoderReranker(top_n=2)` + `HuggingFaceCrossEncoder` — same flip, loop/sort andar chhupe. "Compression"=candidate set ko best top_n me nichodna. Bi-encoder = doc query-blind embed (ingest time) → rough; cross-encoder = accurate par slow, pehle se embed nahi ho sakta → sirf shortlist pe. LIVE: 5 corpus try kiye — **all-MiniLM ne 4/5 me #1 sahi diya** (accha bi-encoder chhote saaf data pe strong → re-ranking OPTIONAL, Day14 jaisa). SonicWave case me flip mila (bi #1=echo-trap D1 0.717, sahi D0 #2 0.686 → L-12 reranker ne D0 #1 +5.01). 3 KEY: (1) bi-encoder aksar sahi=re-rank optional; (2) re-rank ULTA bhi kar sakta (entity-confusion "return window" case me sahi neeche gaya) → eval se prove; (3) reranker QUALITY matter (L-6 fail, L-12 pass). LIBRARY version (02) abhi pending. 1 diagram. Exercise banaya.

- **Day 14** — 🟣 PHASE 4 shuru: HyDE (Hypothetical Document Embeddings). Problem: query(casual sawaal) vs document(formal jawab) roop alag → cosine weak. Fix: query → LLM se FARAZ jawab (document-jaisa) → usse search. Live: query "paisa wapas" normal→Shipping ❌ (scores ~0); HyDE→Refund 0.52 ✅. KEY: faraz jawab galat ho to bhi chalega (dhundhne ke liye, dene ke liye nahi). LLM ab retrieval se PEHLE (query transform). Reality: OPTIONAL (extra LLM call=slow+mehnga; eval se prove karo). Mentor repo me nahi (bonus depth).

- **Day 12-13** — Evaluation (RAGAS = RAG ka report card). eval=RAG's unit test but score(0-1)/LLM-judge vs pass-fail/exact. File1 scratch LLM-as-judge (faithfulness+relevance; hallucinated Rs5000→faith 0.00). File2 real RAGAS (3 metrics; Claude+local emb plugged, `reference` needed for context_precision; `datasets` = eval table). Live: achha faith=1.0, hallucinated faith=0.0 (pakda!); relevancy ~0.45 (weak Hinglish emb — eval khud imperfect, kai metrics dekho). Mentor session-08 RAGAS + hireflow eval_chain (LLM-as-judge for resume-vs-JD scoring, LCEL+Pydantic).

- **Day 11** — Query Routing ("receptionist"). 3 sources (HR/Finance/Tech). File1 embedding-router (cosine to source descriptions; weak model atka on "chhutti"=0.12). File2 LLM-router (Claude) nailed all incl "paisa wapas"→FINANCE (no keyword). 2-level arch: 1 ChromaDB, many collections (router=descriptions index, sources=own collections). KEY: LLM DECIDING (not just answering) = agents ki jhalak → Day 17-18 seed. Mentor session-06-07/04: RouterQueryEngine+LLMSingleSelector, QueryEngineTool (source=tool, agents vocab).

- **Day 10** — LlamaIndex (doosra framework). `Documents→VectorStoreIndex.from_documents (chunk+embed+store ek line)→as_query_engine→query()`. Same PDF+query → LlamaIndex(~4 lines) & LangChain(~10 lines) gave same answer (EMI bounce=₹1000+GST). Philosophy: LangChain=React(control), LlamaIndex=Next.js(convenience). Setup gotcha: py3.9 clash → pinned old version + bridged Claude via LangChainLLM(ChatAnthropic) into LlamaIndex. Mentor (session-06-07): 01_why(LangChain)→02_basic(LlamaIndex, SimpleDirectoryReader).

- **Day 9** — 🟡 PHASE 3 shuru: LangChain **LCEL** (`retriever|prompt|llm|parser`). Built `|` from scratch (operator overloading `__or__` = nested calls saaf). Manual RAGBot(vanilla) → declarative chain(framework). KEY: cosine/MMR/embeddings/Chroma DIDN'T vanish — packed inside `retriever`/`emb`/`Chroma()` lines (framework=wrapper). LangChain = full GenAI/Agentic framework (RAG is one part; CodeSentinel/LangGraph = same family). Mentor scam_guard: `prompt|llm|parser` + **PydanticOutputParser** (structured output = TS interface) + prompt versioning.

- **Day 8** — 🎉 PHASE 2 CAPSTONE: `RAGBot` class (ingest+ask, all 6 skills) + **Streamlit browser chat UI** (`app.py`). Tested LIVE in browser: EMI bounce → ₹1000+GST + page[2,7] citation; pizza → honest "not found" (threshold guard). Engine/UI separation (ApiClient vs component); st.session_state = React useState. Mentor bajajbot: hybrid BM25(sparse)+dense+EnsembleRetriever (Day 15 topic).

- **Day 7** — Retrieval Quality: top_k / threshold / MMR (all scratch + LangChain). Live: k=6 noise, threshold rejected 'pizza' (anti-hallucination), MMR scale-mismatch bug (lam=0.5 no effect, lam=0.1 worked). 🐛 **Caught a real bug in mentor's BFL_chatbot production config** (fetch_k==k kills MMR diversity; 'threshold' ignored in mmr mode) — the win of building scratch-first. Reused Day-6 bajaj_policy collection.

- **Day 6** — Document Loaders. pypdf scratch (binary→text, 12 pages) → PyPDFLoader + CLEAN step (boilerplate!) + split_documents (metadata inheritance) + Chroma → cited search (EMI bounce = Rs1000+GST, PAGE 2). Citations foundation. Mentor: CSVLoader/TextLoader; cleaning was our addition. Exercise solved.

- **Day 5** — Persistence + ChromaDB. Scratch save/load (localStorage pattern) → Chroma (persist free, metadata, where-filter fixed "din" bug live). Real bug caught: relative path → `dirname(__file__)`. Mentor: LangChain wrapper, OpenAI embeddings, Pinecone (cloud vs local).
- **Day 1** — RAG from scratch, working Claude demo + LIVE embeddings preview. Learned retrieve→augment→generate, grounding, and saw real 384-dim vectors + cosine similarity with own eyes.
- **Day 2** — Cosine similarity deep dive. Built it FROM SCRATCH (matched library exactly, 0.2746). Mithai analogy (`[meetha,teekha]`) cracked it. Then built full SEMANTIC RAG (`02_semantic_rag.py`) replacing Day 1's keyword retrieve. Saw retrieval imperfection live (weak Hinglish model picked Support over Refund due to "din"). Notes have 2 embedded SVG diagrams. Exercise SOLVED by learner (all correct).
- **Day 4** — Vector Store / FAISS. Built `MiniVectorStore` class from scratch (add + exhaustive cosine search) → then FAISS (`IndexFlatIP` + `normalize_L2` = cosine). Both gave EXACT same result (0.417 Refund) proving FAISS = same work, just fast/scalable. Concepts: index vs cache, ANN/clustering ("mohalla" trick), O(n) problem. Taught Python class→JS (`__init__`=constructor, `self`=`this`). Mentor comparison (session-04): sir showed IndexFlatL2 (distance), IndexHNSWFlat (real ANN code w/ efConstruction/efSearch), memory calc. requirements.txt now maintained. 1 diagram embedded. Exercise created.
- **Day 3** — Chunking. Learner derived `step = chunk_size - overlap` himself. Scratch char-based chunking (`01_chunking_scratch.py`) showed words breaking (`Yeh`→`Ye`+`h`) + overlap saving context live. Library `RecursiveCharacterTextSplitter` (`02_chunking_library.py`) = clean word-boundary chunks. Analogies: pagination, git diff context lines, CSS word-break. Mentor comparison done (session-03 notebook — sir also did char→word→library; learned word-based chunking as middle ground). Diagram embedded. Exercise created (learner will self-solve).

---

## 🔄 Day Log

| Day | Date | Topic | Status | Notes / Key learnings |
|-----|------|-------|--------|------------------------|
| 1 | 2026-06-29 | RAG mental model + zero-library demo + embeddings preview | ✅ Done | Concept-first session. Learner self-derived: RAG-why (LLM lacks private/fresh data, retrain too costly), retrieval, why keyword search fails ("refund" vs "paisa wapas"), embeddings = meaning→numbers, vector space (similar=near), cosine similarity. Ran `day-01/rag_demo.py` (Claude, grounding works) + `day-01/embeddings_live.py` (real 384-dim vectors; refund doc won 0.463 without the word "refund"). Noted payment scored 0.426 → scores imperfect, motivates better techniques later. See `day-01/notes.md`. |
| 2 | 2026-06-30 | Cosine similarity (scratch) + Semantic RAG | ✅ Done | Built cosine from scratch (dot/magnitude/cosine), matched library (0.2746). Mithai `[meetha,teekha]` analogy. `02_semantic_rag.py` = semantic retrieve + Claude. Live lesson: weak Hinglish model ranked Support>Refund due to word "din"; top_k=2 saved it. 2 SVG diagrams embedded in notes. Exercise solved (all correct). |
| 3 | 2026-07-07 | Chunking (scratch + library) | ✅ Done | `step = chunk_size - overlap` (learner derived). Scratch char-based (words break) vs library `RecursiveCharacterTextSplitter` (clean). Overlap = git diff context lines. Mentor comparison (session-03): sir did char→word→library; word-based chunking = middle ground. 1 SVG diagram. Exercise solved (all correct). |
| 6 | 2026-07-19 | Document Loaders (scratch + library) | ✅ Done | pypdf page-by-page (binary proof: f.read()=kachra). Dirty-data lesson: repeated header/footer → milawat vectors → CLEAN before chunk. `split_documents` = metadata VIRASAT (source+page real, Day-5 fake label ab sach). Full pipeline: PDF→load→clean→chunk(500/80, 40 chunks)→Chroma→search with PAGE citation (EMI bounce Rs1000+GST @ page 2). Mentor (session-03): TextLoader+CSVLoader multi-format; no cleaning (our addition). 1 diagram. Exercise solved (all correct). |
| 7 | 2026-07-21 | Retrieval Quality: top_k/threshold/MMR (scratch + library) | ✅ Done | 3 knobs. top_k drift (k=6 pulls credit-score/balance-transfer noise). Threshold rejects out-of-corpus query ("pizza"→"jawab nahi mila", anti-hallucination); distance-mode KAM=better vs similarity-mode ZYADA=better (tune on real scores: 0.3 wrongly rejected EMI@0.297→0.25). MMR=Maximal Marginal Relevance (thali not 3 katori daal); scratch scale-mismatch (relevance -dist vs repeat cosine, diff scales)→lam=0.5 no effect, lam=0.1 gave p7+p5+p7 diverse. 🐛 Mentor BFL_chatbot bug: fetch_k==k (no MMR choice) + threshold ignored in mmr mode. LangChain normalizes internally. Mentor 03_retreiveal = LlamaIndex (Day10). 1 diagram. Exercise solved (all correct). |
| 22 | 2026-07-29 | Agent layer (differentiator, Day 17-18) (Phase 5 🟠) | ✅ Done | Build-day, LIVE. `/api/chat` ab AGENTIC — tool_used ASLI decision (Day21 hardcode tha). Design: agent tool ko sirf STRING deta → FACTORY+closure (`build_tools(company_id, state)`: company_id bind + sources capture, useCallback jaisa). tools.py: policy_search(RAG wrap)+account_api(mock VJ-100/200), desc=LAKSHMAN REKHA. agent.py: create_tool_calling_agent+AgentExecutor(return_intermediate_steps→tool_used). 🐛 Claude output list-of-blocks→`_as_text()` fix. TEST: EMI→policy_search+cite; VJ-100→account_api; mausam→none honest. Next Day23 React. |
| 21 | 2026-07-28 | Retrieval + Generation → POST /api/chat (Phase 5 🟠) | ✅ Done | Build-day, LIVE. "Answer side" — koi naya RAG concept nahi, WIRING (Day7+8+9) API me. Threshold PROBE-then-tune: in-corpus ~0.42-0.56, out ~0.81+ → 0.7. `retriever.py` (Chroma + company_id filter + threshold), `rag_chain.py` (answer()→(text,sources), DOUBLE grounding guard, LCEL prompt\|ChatAnthropic\|StrOutputParser), `routes.py` POST /chat (tool_used manual: policy_search/none). TEST: EMI→₹1000+GST+cite p7,p2+policy_search; pizza→"jaankari nahi"+[]+none. py3.9 f-string `\` gotcha. Code `capstone/backend/`. Next Day22 agent. |
| 20 | 2026-07-28 | Backend skeleton + Ingestion pipeline (Phase 5 🟠) | ✅ Done | Build-day, LIVE. NAYA: FastAPI=Python Express/Nest (`@app.get`, dict→JSON, uvicorn --reload, free `/docs` Swagger, Pydantic response_model=runtime validation). `main.py`(CORS+router), `config.py`, `routes.py`(/health+POST /upload), `ingestion/pipeline.py`=ingest_pdf(path,company_id)->(doc_id,n): PyPDFLoader(6)→clean→split 500/80(3)→Chroma(5) metadata{company_id,doc_id,source,page}; 1 collection+company_id filter (multi-tenant); embed=SentenceTransformer(all-MiniLM). TEST: bajaj→46 chunks, EMI bounce→p2 ₹1000+GST; isolation (bajaj→2, acme→0 🔒); curl /api/upload→{doc_id,chunks:46,ok}. Code in `capstone/backend/`. FUTURE: dedup. Next Day 21: retrieval+generation→/api/chat. |
| 19 | 2026-07-28 | Design & Architecture (Phase 5 shuru 🟠) | ✅ Done | NO heavy code — blueprint. Decisions locked: repo=`rag-mastery/capstone/`, seed=Bajaj policy (dummy data, app limit nahi). BIG: API contract = single source of truth (Pydantic `models/schemas.py` ⇄ TS `api/types.ts` = 2 mirror sheeshe). 3 endpoints (upload/chat/health; chat→answer+sources+tool_used). `capstone/` hireflow-style skeleton banaya (backend ingestion/retrieval/generation/agent/api/models + frontend api/components/pages); har module docstring "Day X bharega + reuse". capstone = Day 1-18 ka sab ek app me; naya = FastAPI+React+jodna. README=arch doc. Full plan: `docs/capstone-roadmap.md`. Next Day 20: FastAPI+ingestion. |
| 17-18 | 2026-07-28 | Agentic RAG (RAG + tool use) — scratch + library | ✅ Done | PHASE 4 FINALE. Seedhi rassi (Day 8) → agent LOOP: LLM khud decide kaunsa tool + kya input → tool chalao → result wapas → phir tool ya final ANSWER. RAG ab EK tool (`policy_search`) + `account_balance` (fake live API). Tool = name + description(manual: role+input+LIMIT) + function. Asli jaadu (learner ne pakda): LLM raw STRING deta → hum PARSE karke Python fn chalate (scratch regex `TOOL:x\|INPUT:y`; library Claude native tool-call). LIVE: 3 query 3 raaste; input KHUD nikaala (VJ-100); combined=3-step (order LLM decide); VJ-999→"nahi mila"→jhooth nahi=self-correcting (Day8 threshold+Day12 faithfulness andar). Library: `@tool`(docstring=desc)+`ChatAnthropic`+prompt`{agent_scratchpad}`+`create_tool_calling_agent`+`AgentExecutor` (loop andar chhupa; verbose `Invoking:`=scratch TOOL:INPUT:). 🐛 2 experiment: jhoothi/colliding desc→Claude DONO baar sahi (naam+cross-check robust; "kharab=pakka galat" GALAT); under-scoped desc→tool CHALAYA HI NAHI→hallucination (reliable failure). Seekh: description=LAKSHMAN REKHA. Koi nayi lib nahi. 1 diagram. Exercise+bonus solved. TODO: mentor comparison (05_Agentic_AI+bajaj_tools.py). |
| 16 | 2026-07-27 | Multi-Query + Parent-Child chunking (scratch + library) | ✅ Done | Do problem/do fix. Multi-Query=1 query→LLM 3-4 roop→sab search→merge unique (recall↑, cost↑). Parent-Child=search CHILD(chhota=सटीक) par LLM ko PARENT(bada=context); "dhoondha child se diya parent". Ingestion 2-split: PARENTS→docstore(dict/InMemoryStore, no embed), CHILDREN→Chroma embed, child me `parent_id` pointer. Retrieve: child search→parent_id se docstore LOOKUP (search nahi)→merge. LIVE bajaj: "EMI bounce charge?"→4 child→2 parent; answer me CIBIL+agli-EMI (=parent full context, child me nahi=PC point). Lib: ParentDocumentRetriever(parent_splitter=None) nested in MultiQueryRetriever.from_llm. 🐛 default parser intro-line "Here are 3 versions:"→query bani→foreclosure kachra (precision↓); scratch me nahi(strip kiya). FIX: CleanLineParser(`:`/no-`?` chhaan)+prompt\|llm\|parser LCEL→3→2 parent clean. 2 diagram. Exercise banaya. Learner ne top architecture-sawaal poochhe (kab/routing/bada-chunk kahan se). Koi nayi lib nahi. |
| 15 | 2026-07-27 | Re-ranking / 2-stage retrieval (scratch + library) | ✅ Done | Bi-encoder (query&doc ALAG embed, doc ingest-time=query-blind→rough, FAST) → shortlist(extra) → cross-encoder (query+doc SAATH→score, accurate+slow, embed nahi ho sakta→sirf shortlist) → re-sort. shortlist_k>final_k (best doc kahin aa jaaye; bahut zyada=slow). LIVE 5 corpus: all-MiniLM ne 4/5 me #1 sahi (days-trap/echo/return-polysemy/buried-warranty) → re-rank OPTIONAL. SonicWave FLIP: bi #1=echo D1(0.717) sahi D0 #2(0.686)→ L-12 reranker D0 #1(+5.01). L-6 reranker FAIL(echo #1)→L-12 pass=reranker quality matter. Entity-confusion("return window for electronics") me re-rank ULTA(sahi neeche)→eval(Day12) se prove. Model: `cross-encoder/ms-marco-MiniLM-L-12-v2`. Library (File 2): LangChain `ContextualCompressionRetriever`+`CrossEncoderReranker(top_n=2)`+`HuggingFaceCrossEncoder`, base=`as_retriever(k=4)` — same flip, loop andar chhupa ("compression"=best top_n me nichodna). 1 diagram. Exercise banaya. TODO: mentor reranker/Cohere dhoondhna (coding_ninja_genai). |
| 14 | 2026-07-21 | HyDE (Hypothetical Document Embeddings) | ✅ Done | Full form: Hypothetical Document Embeddings = faraz jawab ka embedding. Problem: query(casual) vs doc(formal) roop alag → cosine weak. HyDE: query→LLM faraz jawab(doc-jaisa)→usse retrieve. Live `01_hyde_scratch.py`: "mera paisa kab wapas?" normal→Shipping ❌ (scores ~0!); HyDE faraz "Refund Policy..."→Refund 0.52 ✅. KEY: faraz jawab galat (7-10 din vs 30) chalega — dhundhne ko, dene ko nahi. Naya: LLM retrieval se PEHLE (query transform, pehle sirf end me). Reality: OPTIONAL tool (2x LLM call=slow+mehnga; lagane se pehle eval-Day12 se prove). Mentor repo me nahi=bonus depth. 1 diagram. Exercise solved (all correct). |
| 12-13 | 2026-07-21 | Evaluation / RAGAS (scratch judge + library) | ✅ Done | eval=RAG report card (jargon: eval=evaluation, RAGAS=RAG Assessment, datasets=eval table). vs unit-test: score 0-1 (not pass/fail), LLM-judge (not exact), because LLM output non-deterministic. File1 scratch LLM-as-judge (faithfulness+relevance, 3 cases: good=1.0, hallucinated Rs5000=faith 0.0, off-topic=0). File2 real RAGAS 0.4.3 (faithfulness/answer_relevancy/context_precision; Claude+SentenceTransformer via LangchainLLMWrapper; needs `reference`; cols renamed answer->response). Live: faith caught hallucination (0.00); context_precision=1.0 both (naapta RETRIEVAL not answer); relevancy ~0.45 low = weak Hinglish emb → LESSON: eval khud imperfect, dekho kai metrics. Mentor session-08 rag_assesment (RAGAS) + hireflow eval_chain (LLM-judge resume-vs-JD, LCEL+PydanticOutputParser). 1 diagram. Exercise solved (all correct; D2 trick caught). |
| 11 | 2026-07-21 | Query Routing (scratch embedding + LLM router) | ✅ Done | Router = receptionist / React Router (query MEANING se sahi source). 3 sources HR/Finance/Tech. File1 embedding-router (cosine to descriptions; weak model "chhutti"=0.12 atka, retrieve galat). File2 LLM-router (Claude) = all correct incl "paisa wapas"→FINANCE (no "refund" word). Arch: 1 ChromaDB many collections (router=desc index L1, sources=own collections L2; only chosen retrieved=sharp+fast). BIG: LLM DECIDING not just answering = agents jhalak (Day17-18 seed; CodeSentinel tool-choice link). Mentor 04_routing: per-source Pinecone index + QueryEngineTool(name+desc)=source-as-tool + RouterQueryEngine+LLMSingleSelector. 1 diagram. Exercise solved (all correct). |
| 10 | 2026-07-21 | LlamaIndex + LangChain-vs-LlamaIndex (framework #2) | ✅ Done | LlamaIndex mental model: Documents→VectorStoreIndex.from_documents (chunk+embed+store 1 line)→as_query_engine(similarity_top_k)→query(). File2 side-by-side same query: LangChain ~10 lines vs LlamaIndex ~4, LAGBHAG SAME answer (EMI bounce ₹1000+GST T+1). Philosophy LangChain="control do"(React) vs LlamaIndex="mujhpe chhod"(Next.js). Concepts again hidden (D2/D3/D5/D7/D1 packed). Setup gotcha: py3.9 vs latest LlamaIndex (Path|None) → pin llama-index-core==0.11.23; anthropic SDK clash → bridged via LangChainLLM(ChatAnthropic) (frameworks interconnect). Mentor session-06-07: 01_why=LangChain, 02_basic=LlamaIndex SimpleDirectoryReader+pprint_response(show_source). 1 diagram. Exercise solved (all correct). |
| 9 | 2026-07-21 | LangChain LCEL (scratch pipe + chain) | ✅ Done | `|` scratch via `__or__` operator overloading (parse(llm(prompt(retrieve(q)))) == retriever|prompt|llm|parser). Real LCEL: {context:retriever|format, question:RunnablePassthrough}|prompt|llm|parser; same behaviour as Day-8 (EMI→Rs1000+GST, pizza→"jaankari nahi"). KEY insight: Day1-8 concepts (cosine/MMR/embed/chunk/Chroma) not gone — hidden inside retriever/emb/Chroma() (framework=wrapper; scratch-first=we know inside). LangChain=full GenAI/agentic fw (RAG one part). Mentor scam_guard (production): prompt|llm|parser + PydanticOutputParser (structured/typed output) + prompt registry versioning. Analogy: vanilla JS→React. 1 diagram. Exercise solved (all correct; D1 magic-trick caught). |
| 8 | 2026-07-21 | End-to-end RAGBot + Streamlit UI (CAPSTONE) | ✅ Done | `RAGBot` class: ingest()=load+clean+chunk+store (once, persist-skip), ask()=retrieve+threshold+Claude+citations (per query, returns {answer,sources}). Streamlit `app.py` = browser chat (st.session_state=useState; script re-runs top-to-bottom). LIVE browser test: EMI bounce=Rs1000+GST @page[2,7]; pizza="jaankari nahi mili" (honest). Knobs top-of-file (config/code separation). Mentor session4_bajajbot: hardcoded Documents, HuggingFace emb, +**BM25 sparse + dense + EnsembleRetriever hybrid** (dense=meaning wins on "installment nahi de paunga", BM25=exact wins on code "BFL-PL-2024"; Day 15). 2 diagrams. Exercise solved (all correct; D1 guard-trick caught). |
| 5 | 2026-07-14 | Persistence + ChromaDB (scratch + library) | ✅ Done | Scratch: MiniVectorStore + save/load (json.dump/load = localStorage pattern, .tolist() serialization). LIVE bug: relative path FileNotFoundError → `dirname(__file__)` fix. Chroma: PersistentClient (persist free), collection, add(ids/docs/metadatas), query = DISTANCE (kam=better), `where` filter ne "din" retrieval bug LIVE fix kiya. Metadata = label only (data-attributes). Mentor: LangChain Chroma wrapper + OpenAI embeddings (1536-dim match) + **Pinecone cloud DB** (local vs cloud = localStorage vs Firebase). 1 diagram. Exercise solved (all correct). |
| 4 | 2026-07-08 | Vector Store / FAISS (scratch + library) | ✅ Done | `MiniVectorStore` class scratch → FAISS `IndexFlatIP`+`normalize_L2`. Same result (0.417) = proof. Index vs cache, ANN clustering (mohalla), O(n) problem. Python class→JS (`__init__`/`self`). Mentor (session-04): IndexFlatL2, HNSW real code, memory calc. requirements.txt maintained. 1 diagram. Exercise solved (all correct; D1 trick caught). |

---

## 🧠 Decisions Made
- **Mentor map (2026-07-05):** `docs/mentor-map.md` — har day ka coding_ninja_genai notebook/project mapping. Session flow: scratch first → mentor notebook compare → notes mein "Mentor comparison" section.
- **LLM provider:** Anthropic Claude (model `claude-sonnet-4-6`). API key in `.env`.
- **Embeddings (Day 2+):** will use free local `sentence-transformers` (Anthropic has no embedding model).
- **Python:** 3.9, venv at `rag-mastery/venv/`

## ❓ Open Questions / Stuck Points
_(koi nahi abhi)_

## ▶️ Next session (Day 9 — LangChain LCEL, Phase 3 shuru 🟡)
- Phase 1+2 done (scratch + real pipeline + bot + UI). Ab Phase 3 = FRAMEWORKS.
- Day 9 goal: LangChain LCEL — `prompt | llm | parser` pipe syntax; poore RAGBot ko LCEL chain mein.
- Frontend bridge: pipe/compose (RxJS `pipe()`, unix `|`, function composition).
- Mentor: `03_prompt_engineering/Langchain_learning/` + `Projects/scam_guard/app/chain/scam_chain.py`
  (LCEL production example: prompt|llm|parser).
- Pattern same: scratch (manual chain) → LCEL → mentor compare → notes+diagram+exercise.
- IGNORE old lines below (Day 8 done).
  + Claude generation + citations, apne (ya naye) dataset pe. Yeh Phase-1/2 ka capstone.
- Mentor: `session-04/session4_bajajbot_complete.ipynb` + `Projects/BFL_chatbot/` (full structure).
- Frontend bridge: ek reusable service/module (jaise ek API client class).
- Day 6 wale bajaj_policy Chroma collection ko hi reuse karo (40 chunks ready hain).
- Mentor: `04_RAG_NLP/session-06-07/03_retreiveal.ipynb` + `Projects/BFL_chatbot/app/rag_chain.py`
  (LIVE MMR config: `k`, `fetch_k`, `lambda_mult`).
- Frontend bridge: search ranking / debounce / result de-duplication.
- Pattern same: scratch (threshold/MMR logic khud) → library → mentor compare → notes+diagram+exercise.
